import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vue.blog.example',
  appName: 'vue-blog-example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
